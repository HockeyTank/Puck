<%= form_for(@rink_league) do |f| %>
  <% if @rink_league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rink_league.errors.count, "error") %> prohibited this rink_league from being saved:</h2>

      <ul>
      <% @rink_league.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :display %><br />
    <%= f.text_field :display %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
